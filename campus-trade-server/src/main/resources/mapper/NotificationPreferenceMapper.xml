<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.NotificationPreferenceMapper">
    <select id="selectByUserAndType" resultType="com.campustrade.entity.NotificationPreference">
        SELECT * FROM t_notification_preference WHERE user_id = #{userId} AND notification_type = #{type} AND deleted = 0
    </select>
    <select id="selectByUserId" resultType="com.campustrade.entity.NotificationPreference">
        SELECT * FROM t_notification_preference WHERE user_id = #{userId} AND deleted = 0
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_notification_preference (user_id, notification_type, enabled, create_time, update_time, deleted, version)
        VALUES (#{userId}, #{notificationType}, #{enabled}, NOW(), NOW(), 0, 0)
    </insert>
    <update id="updateEnabled">
        UPDATE t_notification_preference SET enabled = #{enabled}, version = version + 1 WHERE id = #{id}
    </update>
</mapper>