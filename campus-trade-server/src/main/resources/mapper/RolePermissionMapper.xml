<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.RolePermissionMapper">

    <insert id="insert" parameterType="com.campustrade.entity.RolePermission" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_role_permission (role_id, permission_id, create_time, update_time, deleted, version)
        VALUES (#{roleId}, #{permissionId}, NOW(), NOW(), 0, 0)
    </insert>

    <delete id="deleteByRoleId">
        UPDATE t_role_permission SET deleted = 1, update_time = NOW() WHERE role_id = #{roleId} AND deleted = 0
    </delete>

    <select id="selectPermissionIdsByRoleId" resultType="java.lang.Long">
        SELECT permission_id FROM t_role_permission WHERE role_id = #{roleId} AND deleted = 0
    </select>

    <select id="selectByRoleIdAndPermissionId" resultType="com.campustrade.entity.RolePermission">
        SELECT * FROM t_role_permission WHERE role_id = #{roleId} AND permission_id = #{permissionId} AND deleted = 0
    </select>

</mapper>