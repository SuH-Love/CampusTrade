<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.campustrade.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="goods_title" property="goodsTitle"/>
        <result column="goods_image" property="goodsImage"/>
        <result column="price" property="price"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT * FROM t_order_item WHERE order_id = #{orderId} AND deleted = 0
    </select>

    <insert id="insertBatch">
        INSERT INTO t_order_item (order_id, goods_id, goods_title, goods_image, price)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.goodsId}, #{item.goodsTitle}, #{item.goodsImage}, #{item.price})
        </foreach>
    </insert>
</mapper>