<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.GoodsFavoriteMapper">

    <resultMap id="BaseResultMap" type="com.campustrade.entity.GoodsFavorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectByUserAndGoods" resultMap="BaseResultMap">
        SELECT * FROM t_goods_favorite WHERE user_id = #{userId} AND goods_id = #{goodsId} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM t_goods_favorite WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCountByUserId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_goods_favorite WHERE user_id = #{userId} AND deleted = 0
    </select>

    <select id="selectGoodsIdsByUserId" resultType="java.lang.Long">
        SELECT goods_id FROM t_goods_favorite WHERE user_id = #{userId} AND deleted = 0
        ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_goods_favorite (user_id, goods_id, create_time, update_time, deleted, version)
        VALUES (#{userId}, #{goodsId}, NOW(), NOW(), 0, 0)
        ON DUPLICATE KEY UPDATE deleted = 0, update_time = NOW(), version = version + 1
    </insert>

    <delete id="deleteByUserAndGoods">
        DELETE FROM t_goods_favorite WHERE user_id = #{userId} AND goods_id = #{goodsId}
    </delete>
</mapper>