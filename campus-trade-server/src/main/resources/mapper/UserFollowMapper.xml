<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.UserFollowMapper">
    <resultMap id="BaseResultMap" type="com.campustrade.entity.UserFollow">
        <id column="id" property="id"/>
        <result column="follower_id" property="followerId"/>
        <result column="following_id" property="followingId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>
    <select id="selectByFollowerAndFollowing" resultMap="BaseResultMap">
        SELECT * FROM t_user_follow WHERE follower_id = #{followerId} AND following_id = #{followingId} AND deleted = 0
    </select>
    <select id="selectFollowingIds" resultType="java.lang.Long">
        SELECT following_id FROM t_user_follow WHERE follower_id = #{followerId} AND deleted = 0
        ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>
    <select id="selectFollowingCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_user_follow WHERE follower_id = #{followerId} AND deleted = 0
    </select>
    <select id="selectFollowerIds" resultType="java.lang.Long">
        SELECT follower_id FROM t_user_follow WHERE following_id = #{followingId} AND deleted = 0
        ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>
    <select id="selectFollowerCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_user_follow WHERE following_id = #{followingId} AND deleted = 0
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user_follow (follower_id, following_id, create_time, update_time, deleted, version)
        VALUES (#{followerId}, #{followingId}, NOW(), NOW(), 0, 0)
    </insert>
    <update id="deleteByFollowerAndFollowing">
        UPDATE t_user_follow SET deleted = 1 WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </update>
    <update id="restoreByFollowerAndFollowing">
        UPDATE t_user_follow SET deleted = 0, version = version + 1 WHERE follower_id = #{followerId} AND following_id = #{followingId}
    </update>
</mapper>