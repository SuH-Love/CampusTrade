<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.SecurityLogMapper">

    <resultMap id="BaseResultMap" type="com.campustrade.entity.SecurityLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="event_type" property="eventType"/>
        <result column="ip" property="ip"/>
        <result column="detail" property="detail"/>
        <result column="trace_id" property="traceId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM t_security_log WHERE deleted = 0
        <if test="eventType != null and eventType != ''">AND event_type = #{eventType}</if>
        <if test="username != null and username != ''">AND username LIKE CONCAT('%', #{username}, '%')</if>
        ORDER BY create_time DESC LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_security_log WHERE deleted = 0
        <if test="eventType != null and eventType != ''">AND event_type = #{eventType}</if>
        <if test="username != null and username != ''">AND username LIKE CONCAT('%', #{username}, '%')</if>
    </select>

    <select id="selectCountTodayByEventType" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT username) FROM t_security_log WHERE deleted = 0 AND event_type = #{eventType} AND create_time >= CURDATE()
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_security_log (user_id, username, event_type, ip, detail, trace_id)
        VALUES (#{userId}, #{username}, #{eventType}, #{ip}, #{detail}, #{traceId})
    </insert>
</mapper>