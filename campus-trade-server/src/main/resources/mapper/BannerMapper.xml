<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campustrade.mapper.BannerMapper">

    <resultMap id="BaseResultMap" type="com.campustrade.entity.Banner">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="subtitle" property="subtitle"/>
        <result column="image_url" property="imageUrl"/>
        <result column="link_url" property="linkUrl"/>
        <result column="bg_color" property="bgColor"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
        <result column="version" property="version"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM t_banner WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectActive" resultMap="BaseResultMap">
        SELECT * FROM t_banner WHERE deleted = 0 AND status = 1
        ORDER BY sort_order ASC, create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM t_banner WHERE deleted = 0
        ORDER BY sort_order ASC, create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM t_banner WHERE deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_banner (title, subtitle, image_url, link_url, bg_color, sort_order, status, create_time, update_time, deleted, version)
        VALUES (#{title}, #{subtitle}, #{imageUrl}, #{linkUrl}, #{bgColor}, #{sortOrder}, #{status}, NOW(), NOW(), 0, 0)
    </insert>

    <update id="updateById">
        UPDATE t_banner
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="subtitle != null">subtitle = #{subtitle},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="bgColor != null">bg_color = #{bgColor},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            version = version + 1
        </set>
        WHERE id = #{id} AND deleted = 0 AND version = #{version}
    </update>

    <update id="logicDeleteById">
        UPDATE t_banner SET deleted = 1 WHERE id = #{id}
    </update>
</mapper>